# 개인정보 처리 안내

시행일: 2026-09-25 (이후 바뀌면 이 문서 맨 아래 "변경 이력"에 적습니다.)

루미아 브리핑룸(이하 "이 프로그램")은 님블뉴런과 무관한 비공식 팬 제작 도구입니다. 핵심 기능(게임 검출·클립 생성·열람)은 인터넷 없이 내 PC 안에서만 동작하며, 아래의 데이터 전송은 **직접 켠 경우에만** 이루어집니다.

## 1. 기본 원칙

- 전송 기능은 **모두 꺼진 상태로 시작**하고, 첫 실행 화면이나 옵션에서 직접 켜야 합니다. 끄거나 켜지 않아도 기능 제한은 없습니다.
- **영상, 썸네일, 결과표 이미지, 화면 조각은 어떤 경우에도 전송하지 않습니다.**
- 닉네임, 팀원 정보, 클립 제목, 파일 경로 원문, 시청·녹화 시각 원문, 스팀·게임 계정 정보는 전송하지 않습니다.
- 켠 항목만, 앱이 떠 있는 동안 하루에 한 번 묶어서 보냅니다. 서버에 연결할 수 없으면 조용히 나중으로 미루고 프로그램은 그대로 동작합니다.

## 2. 수집하는 항목

| 종류 | 내용 | 켜는 곳 |
|---|---|---|
| 라벨 | 클립을 "교전 / 그 외"로 분류한 값과 붙인 시각, 검출 근거 수치(킬·어시스트 변화, 교전 점수 등), 녹화 해상도·게임 모드·교전 구간의 길이와 위치, 결과 화면의 최종 킬·어시스트 수, 내 캐릭터 이름, 직접 적은 라벨 메모. 스팀 녹화에서 만든 클립과 다시보기 영상에서 만든 클립을 모두 포함합니다(스트리머 이름·영상 파일 정보는 제외) | 라벨 전송 |
| 오류 로그·환경 정보 | 프로그램 오류 기록(오류 종류·발생 위치), 프로그램 버전, OS, CPU·그래픽카드·메모리, 화면 배율, 녹화 해상도·코덱·스팀 녹화 버퍼 길이, 영상 재생(HEVC) 가능 여부, 재생용 영상을 만드는 인코더와 걸린 시간, 게임 분석에 걸린 시간과 게임 길이의 비율, 하드웨어 디코딩 사용 여부, 판독 실패 통계(판독이 비어 있는 클립 수). 닉네임과 경로 속 사용자 이름·폴더 이름은 지운 뒤 보냅니다 | 오류 로그 전송 |
| 진단 정보 | 위 환경 정보와 최근 오류 기록(최대 100건)을 옵션의 "진단 정보 보내기"로 **한 번 직접** 보낸 것. 보내기 전에 내용을 미리 볼 수 있고, 서버가 접수 번호를 알려 줍니다. 오류 로그 전송 설정과 관계없이 이 버튼을 누른 순간에만 보내며 그 뒤로 자동 전송이 켜지지 않습니다. 짧은 표시용 ID(`LUMIA-XXXX-XXXX`)로도 문의를 찾을 수 있습니다 | 버튼을 누른 그 순간에만 |
| 설치 식별자 | 프로그램을 처음 설정할 때 무작위로 만들어진 ID. 계정·닉네임과 연결되지 않고, 옵션의 "보낸 데이터" 항목에서 볼 수 있음 | 위 항목을 보낼 때 함께 |
| 접속 정보 | 서버가 요청을 받을 때 알 수 있는 IP 주소. 로그나 파일로 저장하지 않음 (아래 "접속 정보(IP)" 참고) | 자동 |

**라벨 메모는 자유 입력이므로 닉네임 등 개인정보를 적지 마세요.** 메모는 있는 그대로 전송됩니다.

옵션의 "보낼 내용 미리보기"에서 실제로 보낼 내용(개인정보를 지운 뒤의 모습, 메모 포함)을 전송 전에 볼 수 있습니다. 미리보기를 여는 것만으로는 아무것도 전송되지 않습니다.

## 3. 이용 목적

- 게임 검출기와 교전 분류 정확도 개선(여러 PC·해상도에서의 테스트 데이터)
- 오류 원인 파악과 지원되는 환경 확인
- 문의 처리

이 외의 목적(광고, 이용자 추적, 판매)으로 쓰지 않으며 제3자에게 제공하지 않습니다. 서버에 쌓인 라벨은 운영자가 분석을 위해 자기 PC 로 내려받아 쓸 수 있습니다.

## 4. 보관 기간

| 종류 | 보관 |
|---|---|
| 오류 로그·환경 정보·진단 정보 | 수신 후 **90일** 뒤 서버가 자동 삭제 |
| 라벨 | 영상·닉네임이 없어 개인을 식별할 수 없으므로 삭제를 요청하기 전까지 보관(연구 자료) |
| 접속 정보(IP) | 로그·파일로 **저장하지 않습니다**(아래 참고) |

## 5. 삭제 요청

- 프로그램 옵션 "정보·진단" 탭의 **"보낸 데이터 삭제 요청"**: 내 설치 식별자로 서버에 보낸 라벨·오류 로그·진단 정보를 모두 삭제하고, 전송을 끕니다. 나중에 다시 켜면 처음부터 다시 보냅니다.
- 또는 이메일 **lumia.briefingroom@gmail.com** 로 요청. **삭제 대상은 프로그램 옵션에 표시되는 전체 설치 식별자로만 확인**합니다. 남에게 알려질 수 있는 짧은 표시용 ID 는 삭제 권한으로 쓰지 않습니다.
- 프로그램을 다시 설치하거나 설정 폴더를 지우면 설치 식별자가 새로 만들어지므로, 이전 설치에서 보낸 데이터는 이전 식별자로만 삭제할 수 있습니다.

## 6. 보관 장소와 보안

- 서버는 국내 리전(OCI 춘천)에 있으며 HTTPS 로만 수신합니다.
- 수신 서버에 영상·이미지 저장 기능은 없습니다. 허용하지 않은 필드는 저장하지 않고 버립니다.
- 서버 접근 로그는 남기지 않도록 설정되어 있습니다.

## 7. 접속 정보(IP)와 남용 방지

- 웹 서버의 접근 로그를 끄고 IP 를 파일로 저장하지 않습니다.
- 잘못된 요청이 짧은 시간에 반복되면 서버가 **메모리에서만** IP 별 요청 횟수를 일시적으로 세어 그 IP 를 최대 1시간 차단합니다. 이 정보는 디스크에 쓰지 않고 서버가 다시 시작되면 사라집니다. 차단이 일어나면 운영자에게 IP 앞 두 자리만 가린 형태로 알림이 갑니다.
- 서버 프로그램이 오류를 낼 때 남는 기록에는 IP 가 포함될 수 있습니다. 이 기록은 서버의 컨테이너 로그에만 있고 크기 기준(최대 약 10MB)으로 순환되며 삭제됩니다.

## 8. 문의

lumia.briefingroom@gmail.com

## 9. 변경 이력

- 2026-09-25: 진단 정보 보내기(D14) 제공 시작 — "제공 예정" 표시를 지우고 수집 항목·표시용 ID 설명을 적었다.
- 2026-09-25: 첫 시행 직전에 환경 정보 항목(영상 재생 가능 여부, 인코더·소요 시간, 분석 시간 비율, 하드웨어 디코딩 사용 여부)을 추가했다. 아직 어느 사용자도 이 동의에 답하기 전이라 동의 버전은 그대로다.
- 2026-09-25: 첫 시행. 라벨·오류 로그 전송(D10)과 서버 설정(접근 로그 비활성, 90일 자동 삭제, 요청 제한)이 구현·배포된 시점에 확정.
