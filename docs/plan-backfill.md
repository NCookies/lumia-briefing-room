# 과거 녹화 전체 분석 (로그에 없는 경기 복구)

> 목적: 게임 로그(`Player.log`)에 남지 않은 과거 경기도 스팀 녹화 영상만으로 찾아 클립을 만든다.
> 계기: 친구 PC 에서 어제·엊그제 녹화가 분석되지 않았다. 앱이 경기 목록을 `Player.log`/`Player-prev.log`(최근 게임 실행 2번 분량)에서만
> 얻기 때문이다. 앱을 늦게 설치한 사용자는 스팀에 녹화가 남아 있어도 그 경기가 있었다는 사실을 알 수 없다.
> 진행 상태를 이 문서 §0 에 적는다([CLAUDE.md](../CLAUDE.md)).

## 0. 진행 상태

- [x] **B0. 실측** — 이 PC 의 실제 세션으로 화면만으로 게임을 나눠 로그 정답과 비교(§2)
- [x] **B1. 프레임 추출 스트리밍** — 임시 파일 병합·전체 메모리 적재를 파이프 스트리밍으로(6배속 → 28배속, 메모리 일정). 기존 매치 분석 경로도 빨라진다
- [ ] **B2. 세션 스캔** — 세션의 세그먼트를 읽어 프레임 판독을 캐시하고(이어하기), 게임 경계를 만든다
- [ ] **B3. 중복 제외** — 로그로 이미 알거나 처리한 경기, 이미 클립이 있는 경기는 건너뛴다
- [ ] **B4. 분석 실행기** — 오래된 경기부터, 경기 단위 스테이징 → 완료 시 이동, 취소·이어하기
- [ ] **B5. API** — 미리보기(예상 시간)·시작·상태·취소
- [ ] **B6. UI** — "전체 녹화 분석" 버튼, 시간 안내 확인창, 진행 막대, 취소
- [ ] **B7. 실측·문서** — 이 PC 에서 전체 실행(임시 클립 폴더), README·SPEC 갱신

## 1. 설계

**핵심 아이디어**: 로그 기반 경로는 "경기 시작·종료 시각"만 넘겨받으면 나머지(검출·컷·썸네일·결과 화면 OCR)를 다 한다
(`process_match(session, start, end, …)`). 그래서 새로 만들 것은 **경계를 화면에서 찾는 부분**뿐이다.
다시보기(VOD) 분석이 이미 프레임 판독(`analyze_frame`)과 게임 분할(`split_games`)을 갖고 있다.

```
세션 폴더(bg_1049590_…) ─▶ 세그먼트 키프레임(3초 간격) ─▶ FrameState 판독(캐시) ─▶ split_games ─▶ 게임 구간
   ─▶ 로그·기존 클립과 겹치는 것 제외 ─▶ 오래된 것부터 process_match(스테이징) ─▶ 클립 폴더로 이동
```

- **이터널 리턴 세션만** 본다(폴더 이름의 앱 ID 1049590). 다른 게임 세션은 건너뛴다.
- **경계 보정**: 화면 기준 시작은 로딩이 끝나 HUD 가 뜬 시점이라 로그 시작보다 늦고, 끝은 마지막 인게임 프레임이라 로그 종료(로비 복귀)보다 이르다.
  결과 화면은 경기 끝 뒤에 나오므로 끝에 여유(`POST_GAME_SEC`)를 주되 다음 경기 시작을 넘지 않게 한다.
- **취소 시 "복원"** = 롤백이 아니라 **이어하기 + 경기 단위 스테이징**이다. 이 분석은 원본을 읽기만 하고 완성된 클립은 취소해도 유효하다.
  경기 하나는 임시 폴더에 다 만든 뒤 mp4 → 썸네일 → json 순서로 옮기므로, 중간에 멈추거나 컴퓨터가 꺼져도 깨진 클립이 목록에 뜨지 않는다.
  프레임 판독은 캐시에 이어 쓰므로 다시 누르면 저장된 지점부터 계속한다.
- **링버퍼**: 스팀이 새 녹화를 쌓으면 옛 세그먼트를 지운다. 오래된 것부터 처리하고, 처리 중 사라진 구간은 건너뛰며 알린다.
  (실측: 분석 도중 세그먼트가 801 → 616 개로 줄었다.)
- **게임 중 부하**: CPU 를 많이 쓴다. 확인 창에서 게임을 끄고 실행하라고 안내한다(앱은 이미 낮은 우선순위로 돈다).

## 2. B0 실측 (2026-09-24, 이 PC)

세션 `bg_1049590_20260923_095917`(2560x1440). 로그에 어제 8판이 있지만 링버퍼에 남은 것은 마지막 40분(세그먼트 801개, 5.1GB)이라 2판이 대상이다.

| 게임 | 로그(정답) | 화면으로 찾은 것 | 오차 |
|---|---|---|---|
| 1 | 21:08:29 ~ 21:16:55 | 21:08:53 ~ 21:16:47 | 시작 +24초, 끝 −8초 |
| 2 | 21:20:34 ~ 21:43:49 | 21:20:59 ~ 21:43:32 | 시작 +25초, 끝 −17초 |

- 두 판 모두 찾았고 개수도 맞다(신뢰도 0.99~1.00). 오차는 위에 적은 대로 로딩 시간 때문이라 예상대로다.
- 두 번째 실행에서는 링버퍼가 앞부분을 지워 616개가 남았고, 게임 1 은 남은 구간이 잘려 2.5분으로 나왔다 — **잘린 경기는 클립이 부분적으로만 만들어진다**(처리 대상에서 어떻게 다룰지는 B4).
- 속도: 병합 방식 400초(6배속, 프레임 데이터 9GB 메모리) → 파이프 스트리밍 67초(28배속, 프레임 판독 포함, 메모리 일정).
  파이프 디코딩만 재면 64배속이고 병목은 디스크 읽기(112MB/s)다. 4시간 버퍼(약 40GB)는 디스크 기준 6분 안팎으로 **추정**한다(실측 아님).

## 3. 확인 필요

1. **스팀 녹화에서 게임 분할 정확도** — 다시보기에서는 실측했지만 스팀 녹화는 이 PC 의 2판뿐이다. 더 긴 세션·여러 판으로 확인한다.
2. **결과 화면 판독** — 화면 기준 끝에 여유를 준 경계에서 결과 화면(순위)이 읽히는지.
3. **다른 해상도** — 친구 PC 등에서. 1920x1080 은 다시보기로 측정했다.
4. **부분적으로 잘린 경기** — 시작이 링버퍼에 지워진 경기를 클립으로 만들지(불완전 표시) 건너뛸지.
