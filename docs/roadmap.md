# 로드맵 (총괄)

> **이 문서는 "지금 무엇이 남았고 무엇부터 할지"만 다룬다.** 설계·실측·결정의 상세는 각 plan 문서에 있고, 여기서는 링크만 건다.
> 작업 단계를 끝낼 때마다 이 문서의 상태와 우선순위를 같이 갱신한다(CLAUDE.md). 마지막 정리: 2026-09-25.

## 1. 한눈에 보기

| 문서 | 다루는 것 | 상태 | 남은 것 |
|---|---|---|---|
| [plan.md](plan.md) | 검출기(1단계) | ✅ 완료 | 정식 라벨셋 튜닝(사람 작업), 탈출·일반 결과 화면 실측, 숫자 본보기 보강 |
| [plan-pipeline.md](plan-pipeline.md) | 자동화(2단계) | ✅ 완료 | 없음 (3-2 `session.mpd` 상태만 실사용 관찰) |
| [plan-ui.md](plan-ui.md) | 열람 UI(3단계) | ✅ 거의 완료 | 판독값 수동 보정(설계만), pywebview `.localhost` 실측, Playwright HEVC 재생 확인 |
| [plan-pvp.md](plan-pvp.md) | PvP 판별(4단계) | ✅ 구현, 튜닝 진행 | 라벨 확인, 증거 없는 교전용 신호, 후순위 신호들 |
| [plan-vod.md](plan-vod.md) | 다시보기(5단계) | ✅ V0~V5 | V6(평가 도구 `--source`), 다른 스트리머 검증 |
| [plan-backfill.md](plan-backfill.md) | 과거 녹화 분석 | ✅ B0~B7 | 다른 PC·4시간 규모 검증 |
| [plan-deploy.md](plan-deploy.md) | 배포 | 🔶 D1~D6 완료 | **D7·D8·D9·D10·D11·D12·D13·D14** — 지금 주 작업 |
| [plan-infra.md](plan-infra.md) + `infra` | 수신 서버 | 🔶 I1~I3 완료 | I4(클라이언트 연결), I5(DB), 운영 위험 |
| [SPEC.md](SPEC.md) | 설계 결정 | — | §6 "0.5단계 — 남은 확인" 체크박스가 오래됨(§5 참고) |

지금 개발의 무게 중심은 **배포(plan-deploy)와 서버(plan-infra)** 다. 나머지 plan 은 기능이 이미 돌아가고 있고, 남은 것은 대부분 "실사용·친구 배포 피드백이 있어야 진행되는 검증"이다.

## 2. 우선순위

기준: ① 다른 작업을 막는가 ② 사용자(친구)에게 나가기 전에 반드시 필요한가 ③ 비용 대비 효과. 한 번에 하나씩, 위에서 아래로 진행한다.

### P0 — 친구 배포(D8)를 시작하기 위한 것

| # | 작업 | 이유 | 크기 |
|---|---|---|---|
| 1 | **Windows 샌드박스 검증** ([deploy §10](plan-deploy.md)) — **우선순위 낮춤** | 친구 PC 에서 재생이 됐으므로 D8 을 막지는 않는다. 다만 친구 PC 에 HEVC 코덱이 있었는지 모르면 **HEVC 없는 PC 의 프록시 자동 전환·`h264_mf` 존재는 여전히 미실측**이다(공개 Release 전에는 확인 권장). 사용자가 직접 하는 수동 작업 | 소 |
| 2 | ✅ **D11 화면에 버전 표시 완료** ([deploy D11](plan-deploy.md)) | 프론트만 바꾸는 작은 작업. 친구가 보내는 피드백에 버전이 붙어야 한다 | 소 |
| 3 | ✅ **D12 동의 기본값 전부 꺼짐 + 라벨링 UI 숨김 + 라벨 메모 완료** ([deploy D12](plan-deploy.md)) — 다음은 3-b | D3 의 첫 실행 화면을 고치는 재작업이라 **친구가 한 번 본 화면을 다시 바꾸기 전에** 끝내는 게 낫다. D10 의 전제이기도 하다 | 중 |
| 3-b | **UI·API·문서의 어색한 한국어 전수 검수** | 영어를 직역한 듯한 문장이 가끔 보인다는 사용자 보고. 이전 검수(plan-ui §0 "UI 문구 전수 검수", 합니다체 통일) 이후에 첫 실행 화면·정보·진단·프록시 안내·업데이트 등 문구가 많이 늘었다. 범위: `frontend/src` 의 화면 문구, `api/`·`pipeline/` 이 사용자에게 돌려주는 오류 문장, 트레이 메뉴, 설치기(`installer/lumia.iss`), README·friend-guide·privacy 의 사용자용 문서. D12 가 라벨 문구를 바꾸므로 **D12 뒤, 공개 Release(D7) 전에** 한다. 문구 목록을 표(위치·현재·제안)로 먼저 보여 주고 승인받은 뒤 고친다 | 중 |
| 4 | ✅ **D8 친구 배포 1차 완료** (카카오톡으로 exe 전달) | **확인됨: 친구 PC 에서 클립 추출·재생이 정상.** 친구 모니터 해상도가 내 PC 와 같아 **해상도별 동작은 확인하지 못했다.** 아래 "D8 2차" 참고 | 완료 |
| 4-b | **D8 2차 — 다른 해상도·환경 검증** | 1080p 프로필은 측정했지만 스팀 녹화로는 검증 못 했고, 16:9 가 아닌 모니터는 미검증([deploy §6](plan-deploy.md)). 해상도가 다른 사람이 생기면 그때 배포·진단 zip 수집. 탈출·일반 결과 화면, 게임 중 렉, 백신 경고도 같은 방법으로 | 기회가 올 때 |

### P1 — 서버 연결 (D8 피드백을 받는 동안 병행)

| # | 작업 | 이유 | 크기 |
|---|---|---|---|
| 5 | **개인정보 처리 안내·보관 기간·삭제 요청 방법 확정** ([infra §6-5](plan-infra.md)) | 데이터를 받기 **전에** 정해야 한다(국내 이용자). 코드보다 결정이 먼저라 구현 착수 전에 사용자 결정 필요 | 결정 |
| 6 | **전송 계약 확정 + 서버 스키마 대조** ([infra §7](plan-infra.md)) — 필드 목록은 [§9](#9-전송-데이터-명세-보강안-2026-09-25-점검) | 서버 `schemas.py` 의 라벨 허용 필드가 "추정"이다. 실제 `.labels/*.json` 필드와 대조하고 `labelNote`·진단 번들 업로드 경로·접수 번호 응답을 서버에 추가한 뒤 계약을 plan-deploy 에 링크. **개발/배포 모드 구분(`mode` 필드, 개발 모드는 기본적으로 운영 서버에 전송하지 않음)도 이때 정한다** | 중 |
| 7 | **D10 + I4 라벨·오류 로그 전송 클라이언트** (+ [§7 테스트 자동화](#7-서버-연동-기능의-테스트-자동화)) | 라벨은 분류기 튜닝의 재료(plan.md §9-8 의 "사람 라벨링 필요"를 여러 PC 의 라벨로 대체). 계약 테스트·가짜 서버 테스트로 서버 장애가 앱을 멈추지 않는 것을 고정 | 대 |
| 8 | **D14 진단 정보 서버 전송** (표시용 ID·접수 번호) | 친구 피드백 수집을 zip 첨부에서 버튼 하나로. zip 은 대안으로 남긴다 | 중 |

### P2 — 공개·자동화 (공개 저장소 결정 이후)

| # | 작업 | 이유 |
|---|---|---|
| 9 | **D7 공개 저장소 정리** (`_samples` 이미지·git 이력, LICENSE, 사용자용 README, 비공식 고지) | **결정됨: 저장소를 공개하고 GitHub Release 로 배포한다.** 첫 공개 Release 전에 끝나야 한다. 이력 정리가 가장 번거롭다. 게임사 지침 검토 결과는 [§6](#6-결정-사항과-남은-결정) |
| 10 | **D13 배포 자동화** (태그 → 빌드 → Release → VirusTotal) | 공개 저장소가 되면 VirusTotal 을 켜도 된다([deploy §7-17](plan-deploy.md)) |
| 11 | **D9 자동 업데이트(+ 수동 "지금 확인")** | Release 가 있어야 실측할 수 있다. 같은 `AppId` 덮어쓰기 설치는 **아직 실측한 적이 없으므로** 버전을 올려 실제로 확인해야 한다 |

### P3 — 서버 운영 보강 (I4 와 나란히, 급하지 않음)

- 요청 빈도 제한 (현재 없음), 공인 IP 예약 IP 로 전환(VM 재생성 시 도메인이 끊긴다), Terraform 상태 파일 백업
- watchtower 간격 5분 → 1~2시간(`WATCHTOWER_POLL_INTERVAL` 환경변수화)
- 과금·트래픽 알림([infra §6-7](plan-infra.md)), 디스크 용량 감시. 메모리 1GB 에 컨테이너 3개
- **I5 파일 → DB 이전** — 라벨이 실제로 쌓이기 시작한 뒤. DB 종류는 그때 정한다

### P4 — 품질·기능 (배포와 무관, 피드백이 쌓이면 재정렬)

| 작업 | 문서 | 메모 |
|---|---|---|
| 판독값 수동 보정(내·팀원 캐릭터·순위) | [plan-ui §0](plan-ui.md), SPEC §2.13 | 친구 PC 에서 오판독이 나오면 필요성이 커진다 |
| 탈출·일반 결과 화면 실측 | [plan §9](plan.md) | 샘플이 없어 친구 배포를 기다린다 |
| 다른 해상도·16:9 아닌 비율 | [deploy §6](plan-deploy.md), [plan §8-7](plan.md) | 친구 배포 피드백 |
| 잘린 경기(링버퍼에 앞부분이 지워진) 처리, 4시간 규모 | [backfill §6](plan-backfill.md) | 친구 PC |
| 게임 중 분석 부하 → 프레임 저하 | [deploy §7-14](plan-deploy.md) | 친구에게 체감 질문 |
| 정식 라벨셋·파라미터 확정 | [plan §9-8](plan.md), [plan-pvp](plan-pvp.md) | D10 라벨이 쌓이면 대체 |
| VOD V6 (평가 도구 `--source`) | [plan-vod §0](plan-vod.md) | 라벨 활용 |
| 활성 효과 아이콘, 전멸 배너, 네임플레이트, 캐릭터별 특성 | [plan-pvp §0](plan-pvp.md) | 후순위, 신호가 필요할 때 |
| 게임 패치 대비 데이터만 받는 업데이트 | [deploy D9](plan-deploy.md) | 패치로 HUD 가 바뀌면 검출이 깨진다. 실제 위험이라 D9 뒤에 |
| pywebview `.localhost` 실측 | [ui §4-3](plan-ui.md) | 트레이 경로는 기본 브라우저라 영향 작음 |
| **프록시 미리 만들기 + 코덱 설치 안내** (**진행 결정, D11·D12 다음**) | [deploy D4](plan-deploy.md) | 지금은 클립마다 **처음 재생할 때** 프록시를 만들어 기다린다(클립 길이의 약 1/3, 171초 클립 ≈ 50초. 이후는 캐시). 결정: ① 현재 클립이 준비되면 **다음 클립 프록시를 백그라운드로 미리 생성**(프록시 모드일 때만, 직접 연 클립이 미리 만들기보다 우선 — 서버 `Semaphore(1)` 우선순위 수정 필요). ② **옵션으로 껐다 켤 수 있게**(설정 키 신설, 기본 켬). ③ 근본 해결인 코덱 설치는 **자동 설치 불가**(스토어 유료·라이선스, [deploy §7 D4](plan-deploy.md))라 프록시 대기 화면에 **선택적 설치 안내 링크**만 둔다(유료 `9NMZLZ57R3T7` $0.99 / 무료 제조사판 `9N4WGH0Z6VHQ` 는 기기에 따라 제공 안 될 수 있음). 클립 생성 직후 생성은 게임 중 CPU 부담이라 하지 않는다 |

## 3. 추천 진행 순서 요약

```
샌드박스 검증 → D11 → D12 → (D8 친구 배포 시작, zip 수집)
                                   ↓ 병행
        개인정보·계약 결정 → 서버 스키마 대조 → D10+I4 → D14
                                   ↓
        D7(공개 정리) → D13(Release 자동화) → D9(자동 업데이트)     (서버 운영 보강은 틈틈이, I5 는 데이터를 본 뒤)
```

D8 을 D10·D14 보다 **먼저** 시작하는 이유: 남은 미확인 항목의 대부분(해상도, 탈출 화면, 게임 중 부하, 백신 경고)이 친구 PC 에서만 확인되고, 전송 기능은 `consent.version` 을 올려 나중에 새 항목만 다시 묻는 구조라 뒤에 붙여도 된다.

## 4. 추가로 필요해 보이는 것 (기존 plan 에 없었거나 약했던 것)

1. ~~릴리스 전용 공개 저장소 안~~ → 검토했으나 채택하지 않음. 저장소를 공개하고 Release 로 배포하기로 결정([§6](#6-결정-사항과-남은-결정)). 그래서 D7(이력 정리)이 D13·D9 의 선결 조건이다.
2. **서버 API 토큰 회수 방법.** 공유 토큰이 설치기에 들어가므로 유출되면 남용된다. 토큰을 바꾸면 이미 배포된 앱이 전부 전송에 실패한다. 서버가 옛 토큰을 일정 기간 함께 허용하거나, 앱이 토큰을 서버에서 받아 가는 구조가 필요한지 정한다. 빈도 제한(P3)과 같이 본다.
3. **서버에 쌓인 라벨을 로컬로 가져오는 도구.** [infra §3](plan-infra.md) 는 "조회·분석은 서버 밖 로컬 도구"라고만 적고 그 도구는 계획에 없다. `tools/pull_labels.py`(서버 → 로컬 `eval_pvp.py` 입력 형태)가 없으면 D10 이 끝나도 라벨을 튜닝에 못 쓴다.
4. **전송 실패·오프라인 동작의 테스트.** "서버 장애가 앱을 멈추지 않는다"([infra §3](plan-infra.md))는 원칙만 있고 검증 계획이 없다. 대기분 상한(20MB)·재시도 간격·서버 응답 지연 시 UI 무반응 여부를 D10 테스트에 포함한다.
5. **전송 전 개인정보 제거의 회귀 테스트.** 오류 로그·진단 번들에서 닉네임·경로 속 사용자 이름을 지우는 규칙이 자동 전송(D10)으로 넘어가면 실수의 비용이 커진다. 실제 로그 샘플 기반 테스트를 D10·D14 에 넣는다.
6. **업그레이드 설치 실측과 버전 표기 정리.** [deploy §12](plan-deploy.md) 는 `0.1.1` 설치기를 언급하면서 같은 절에서 "버전은 아직 `0.1.0` 하나뿐"이라고 적는다. 지금 어느 쪽인지 확인하고 D9 전에 `0.1.x → 0.1.(x+1)` 덮어쓰기를 한 번 해 본다.
7. **친구 배포 피드백을 담는 곳.** 진단 zip·구두 피드백을 어디에 모을지(표·이슈)가 정해져 있지 않다. 친구 안내문의 확인 표가 그대로 결과표가 되므로 회수 방식만 정하면 된다.
8. **서버 운영 최소 절차.** 서버 재부팅·docker 재기동·인증서 갱신 실패·디스크 가득 참 시 대응을 [infra README]((infra repository)) 의 운영 절에 짧게 정리한다(1GB 메모리에서 OOM 가능성 포함).
9. **핵심 위험 하나: 게임 패치.** HUD 가 바뀌면 배지·K/A·지역명 검출이 조용히 깨진다. 검출 실패 통계(진단 번들에 포함하는 "판독 실패 통계")가 이 신호의 조기 경보가 되도록 D10·D14 에서 항목을 확정한다.

## 5. 문서 정리 필요 (내용이 서로 어긋난 곳)

코드 변경은 없고 문서만 고칠 것들이다. 필요하면 `/todo-sync` 식으로 한 번에 정리한다.

- [SPEC §6 "0.5단계 — 남은 확인"](SPEC.md) 체크박스가 전부 미체크인데 일부는 이미 해결됐다 — "결과 화면 판독"(§2.13 구현), "다른 해상도 프로필"(1080p 추가), "WebView2 HEVC 재생"(plan-ui §4-2 실측). 남은 것(관전 화면 UI, 부활 후 HUD, 멀티킬 거동)만 남기고 정리.
- [plan-pvp.md §0](plan-pvp.md) 의 24행 "다음: 새 경계로 재처리 + 라벨 이관" 이 20행에서 이미 `[x]` 로 완료된 항목과 중복이다.
- [plan-vod.md §0](plan-vod.md) 첫 단락 "실행 방법(README)은 아직 바뀌지 않았다" 가 실제 README 와 맞는지 확인.
- [plan.md §8](plan.md) 의 "확인 필요"는 §9 에서 대부분 해소됐으나 §8-1·§8-4·§8-5 표기가 옛 상태 그대로.
- [plan-deploy.md §0](plan-deploy.md) 체크박스 순서가 D14 → D13 이라 번호 순이 아니다.
- [plan-infra.md §0](plan-infra.md) 의 I4·I5 와 [plan-deploy](plan-deploy.md) D10·D14 가 같은 일을 두 문서에서 따로 추적한다 — 진행 상태는 이 로드맵의 P1 표를 기준으로 삼고 각 문서는 링크만 남기는 것을 권한다.

## 6. 결정 사항과 남은 결정

**결정됨 (2026-09-25)**

- 저장소는 **공개**한다. 정식 배포는 **GitHub Release**. 친구 테스트는 지금까지처럼 카카오톡으로 exe 전달(첫 공개 Release 전까지). [§4-1](#4-추가로-필요해-보이는-것-기존-plan-에-없었거나-약했던-것)의 "릴리스 전용 저장소" 안은 채택하지 않는다.

**게임사 지침 검토 (2026-09-25, 영문 공식 문서 기준 — 법률 검토 아님)**

읽은 것: [IP 이용 정책(2025-08-14 시행)](https://support.playeternalreturn.com/hc/en-us/articles/49503976113177), [EULA](https://support.playeternalreturn.com/hc/en-us/articles/900003100126), [운영 정책(2026-03-04)](https://support.playeternalreturn.com/hc/en-us/articles/54055653655833).

- **프로그램(도구)에 대한 명시적 허용·금지 조항은 없다.** IP 정책은 팬킷·2차 창작물을 다루고, 영상 클립·썸네일·리뷰를 비영리 목적으로 쓰는 것은 허용한다(§3.2).
- **금지되는 것 중 해당될 수 있는 것**: 공식 로고·이름·UI의 무단 사용·모방, 공식으로 오인될 표현(IP §2.2), 이름·마크를 도메인·조직명에 쓰는 것(EULA §3.2), 게임 자산 복제·재배포(EULA §3.1). 클라이언트·데이터 변조와 부정 이득 프로그램(EULA §2, 운영 정책 (6))은 게임 프로세스·메모리·패킷을 건드리지 않고 스팀이 저장한 파일과 `Player.log` 만 읽으며 경기 중 이득이 없으므로 해당하지 않는다고 본다.
- **공개 전에 할 것**: ① `_samples` 게임 화면 이미지 제거(D7) ② README·앱에 "비공식 도구, 님블뉴런과 무관" 고지 ③ 공식 로고·아이콘·UI 이미지를 앱 아이콘·문서에 쓰지 않았는지 확인 ④ 이름의 "Lumia"는 게임 안 지명이라 도메인(`server.example.invalid`)·앱 이름이 EULA §3.2 의 "혼동 가능한 마크"로 읽힐 여지가 작게나마 있다 — 위험은 낮다고 보지만 확신하려면 다음 문의로 확인 ⑤ 본보기 `.npz`(글자 조각)와 `characters.json` 은 원본 이미지는 아니나 회색지대라 문의에 포함
- **확실히 하고 싶으면**: 님블뉴런 고객센터에 "스팀 녹화본을 읽어 클립을 만드는 무료 오픈소스 도구를 공개해도 되는지" 한 번 문의(답변은 근거로 보관). 필수는 아니다.

**남은 결정**

1. 개인정보 처리 안내 확정 ([infra §6-5](plan-infra.md)) — **서버 리전은 국내(국외 이전 고지 불필요), 연락 이메일은 `lumia.briefingroom@gmail.com` 으로 정해졌다.** 초안 [privacy.md](privacy.md) 를 작성했고, 아래 §8 의 제안(IP 접근 로그 삭제·90일 자동 삭제)이 서버에 구현된 뒤 확정·링크한다. 이 서버 쪽 구현이 P3 의 선행 작업이다

**위임받아 판단한 것 (2026-09-25, 저작권 관련) — 위 "확실히 하고 싶으면" 문의는 하지 않기로 했다**

- **앱 라이선스: MIT.** 무료 도구이고 상용 의도가 없으며, 번들하는 pystray(LGPL v3)·ffmpeg(LGPL 빌드)와 충돌하지 않는다(둘 다 `THIRD_PARTY_NOTICES` 에 고지, ffmpeg 는 별도 exe·DLL 이라 교체 가능. pystray 교체 가능성 확인은 [deploy §7-15](plan-deploy.md)).
- **게임사 사전 문의는 하지 않는다.** 도구에 대한 금지 조항이 없고 게임 프로세스를 건드리지 않아 위험이 낮다고 본다. 대신 위 공개 전 체크리스트(`_samples` 제거, 비공식 고지, 공식 로고·UI 이미지 미사용)를 D7 에서 반드시 지키고, 연락·삭제 요청이 오면 바로 응할 수 있게 README 에 연락처를 둔다.
- **본보기 `.npz`·`characters.json` 은 그대로 포함한다.** 원본 화면 이미지가 아니고 실행에 필수다. 문제 제기가 오면 그때 제거하고, 사용자가 자기 녹화에서 본보기를 만드는 `tools/build_templates.py` 가 대안이 된다.
- **Release 설치기·README 고지**: `THIRD_PARTY_NOTICES` + "비공식 팬 제작 도구이며 님블뉴런과 무관합니다".

**미룬 결정: DB 종류.** 데이터가 실제로 들어오는 형식과 양을 본 뒤에 정한다. 사용자 수를 알 수 없으므로 I5 는 **파일 저장으로 충분한 동안 하지 않는다.** 착수 기준: ① 라벨 파일이 수천 개를 넘어 조건 조회(해상도·버전·라벨)가 불편해지거나 ② 로그 검색·집계 요구가 생겼을 때. 그때까지 로컬 도구(§4-3)로 파일을 내려받아 분석한다. 판단 자료로 서버에 일별 수신 건수·평균 크기·필드 변형을 세는 간단한 집계 로그를 남겨 둔다.

## 7. 서버 연동 기능의 테스트 자동화

D9·D10·D14 는 네트워크·설치기·서버가 얽혀 손으로 확인하기 어렵고, 잘못되면 **조용히 안 보내지거나(데이터 유실) 개인정보가 새는** 종류다. 기능과 같은 커밋에서 자동 테스트를 함께 만든다(TDD). 진짜 외부 서비스(GitHub·운영 서버)는 기본 테스트에서 호출하지 않는다.

| 층 | 무엇을 | 방법 |
|---|---|---|
| **계약 테스트** | 앱이 보내는 페이로드가 서버 스키마와 맞는가(`labelNote` 추가·필드 허용 목록 대조 포함) | 계약을 한 곳(JSON Schema 또는 공용 픽스처)에 두고, 이 저장소는 그 픽스처로 페이로드를 만들어 검증, infra 저장소의 수신 API 테스트는 같은 픽스처로 수락·거부를 검증. 스키마가 어긋나면 어느 한쪽 CI 가 깨진다 |
| **클라이언트 단위** | 대기 큐, 하루 1회 묶음, 20MB 상한, 재시도·백오프, 실패해도 앱이 안 멈춤, 동의 꺼짐이면 네트워크 호출 0회, 개인정보 제거 | `httpx.MockTransport` 로 가짜 서버(200·401·413·500·타임아웃·연결 거부). 시계·sleep 주입(기존 `watcher` 방식). 개인정보 제거는 실제 로그 샘플 기반 회귀 테스트 |
| **업데이트 흐름** | 최신 버전 판별, 새 버전 알림, 다운로드 → SHA-256 검증 → 설치기 실행, 체크섬 불일치 거부, `update.check` 꺼짐이면 자동 확인 안 함·수동 확인은 동작 | GitHub Releases API 모양의 가짜 JSON 과 로컬 HTTP 서버로 설치기(더미 파일) 서빙. 설치기 실행은 함수 주입으로 대체 |
| **서버 통합** | 실제 수신 API 컨테이너에 앱 클라이언트로 보내 저장·삭제·접수 번호까지 | infra 저장소의 pytest 를 `docker compose` 로 띄운 서버에 실행하고, 이 저장소의 전송 클라이언트를 같은 서버에 붙이는 통합 테스트를 로컬·CI 에서 선택 실행(`-m integration`) |
| **운영 스모크** | 배포된 서버가 살아 있는가 | 읽기 전용 확인만: `https://server.example.invalid` 헬스체크·토큰 없음 401·`/docs` 404. 데이터를 쓰는 요청은 하지 않는다. 필요하면 정기 실행 |
| **릴리스 파이프라인** | 태그와 `__version__` 불일치 시 실패, `.sha256` 생성, 설치기 이름 규칙 | 순수 함수 테스트 + 워크플로에서 실제 실행(D13) |

원칙: ① 시간·네트워크·설치기 실행은 주입해 실시간 지연 없이 테스트한다(기존 `pipeline/watcher` 방식) ② 서버 장애 시나리오(타임아웃·500·오프라인)는 전부 자동 테스트로 고정한다 ③ 업그레이드 덮어쓰기 설치처럼 자동화가 어려운 것은 [deploy §10](plan-deploy.md) 수동 체크리스트에 남긴다.

## 8. 개인정보 처리 제안안 (확정 전)

법률 자문이 아니라 최소 수집 원칙에 따른 제안이다. 확정하면 `docs/privacy.md` 로 만들어 첫 실행 화면·README 에서 링크한다.

- **서버가 실제로 보는 개인정보성 값은 IP 주소다.** 앱이 보내는 값은 무작위 `installId` 뿐이지만 caddy·receiver 접근 로그에 IP 가 남는다. → 접근 로그에서 IP 를 남기지 않거나 7~14일 후 자동 삭제.
- **보관 기간**: 오류 로그·진단 번들 90일 후 자동 삭제, 라벨은 연구 자료라 삭제 요청 전까지 보관(영상·닉네임이 없어 식별 불가). 기간은 안내문에 그대로 적는다.
- **삭제 요청**: 앱 옵션의 "보낸 데이터 삭제 요청"(전체 `installId` 기준, D10) + 연락 이메일 하나.
- **안내문에 넣을 것**: 수집 항목, 목적(검출기 개선), 보관 기간, 제3자 제공 없음, 서버 위치(OCI 리전이 국외면 국외 이전 고지), 삭제 방법, 연락처, 옵트인이며 꺼도 기능 제한 없음.
- 미성년 이용자가 있을 수 있으나 전부 옵트인·비식별이라 별도 절차는 두지 않는 안.

## 9. 전송 데이터 명세 보강안 (2026-09-25 점검)

서버 수신 스키마(`infra\services\receiver\app\schemas.py`)를 실제 클립 메타데이터(`clips/*.json`, `.labels/*.json`)와 대조했다. **지금 스키마로는 데이터만 보고 분석·판단하기 어렵고, 일부는 앱이 보내는 값을 거부한다.** P1 6번(계약 확정)에서 아래를 반영한다. 원칙은 그대로다: 영상·이미지·닉네임·팀원 정보·경로 원문은 보내지 않는다.

**방침(2026-09-25 결정): 버그 리포트용 오류 로그와 라벨 데이터는 위 금지 항목을 뺀 범위에서 최대한 자세히 보낸다.** 개인정보가 섞이지 않는 한 필드를 아끼지 않는다. 대신 (1) 페이로드에 `schemaVersion` 을 넣고, (2) 서버가 허용 목록에 없어 버린 필드 이름을 집계해 앱·서버 스키마가 어긋나면 알아챌 수 있게 하며, (3) 자유 텍스트(로그 메시지·라벨 메모)는 개인정보 제거 규칙과 회귀 테스트를 통과한 것만 보낸다.

### 라벨

**바로 고칠 것 (버그성)**
- `userLabel` 허용값이 `pvp/hunt/none` 인데 앱은 `pvp/pve` 를 저장한다. **서버가 `pve` 라벨을 검증 실패로 거부한다.**
- **라벨 분류는 `교전` / `그 외` 두 가지로 확정한다(2026-09-25 결정).** 그 외에는 사냥뿐 아니라 이동·대치·대기 장면이 들어간다. 로컬 저장값은 기존 `pvp`/`pve` 를 유지해 이미 붙인 라벨·`.labels/` 보관소·`eval_pvp.py` 를 마이그레이션하지 않는다(과거의 `pve` 는 "사냥"이었지만 새 정의 "그 외"의 부분집합이라 그대로 쓸 수 있다). **전송할 때는 중립 값 `combat`/`other` 로 변환**하고 서버 스키마도 이 값으로 정의한다. 나중에 세분류(사냥·대치·이동 등)가 필요해지면 서버 필드를 추가한다(`labelSchemaVersion` 포함).

**분석에 필요한데 빠진 것**
- 재현 조건: `sourceWidth`·`sourceHeight`(해상도별 정확도 비교의 핵심), `gameMode`, `sourceIncomplete`(잘린 경기 표시), `prerollSource`
- 검증용 정답값: `matchKills`·`matchAssists`·`matchTeamKills`(결과 화면 최종 K/A — 검출값 `killDelta` 와 대조해 판독 오류를 찾는 근거)
- 교전 구간 길이·위치: `combatStartOffsetSec`·`combatEndOffsetSec`(길이만 있고 위치가 없다)
- 같은 경기 묶기·중복 방지: **익명 경기 키**(`sessionDir`+`matchStartUtc` 의 해시)와 **클립 키**(해시). 없으면 같은 경기의 클립을 묶을 수 없고, 라벨을 고쳐 다시 보낼 때 중복 저장된다(서버는 `installId`+클립 키로 덮어쓰기)
- 라벨링 맥락: `labelNote`(D12), 라벨을 붙인 시각, `labelConflict`(라벨과 검출 근거 충돌 표시), 라벨 출처(`labelSource` — 서버에는 있으나 앱 메타데이터에는 없으니 값 정의 필요)
- 모드: `mode`(`dev`/`release`) — 개발 PC 의 라벨이 운영 데이터에 섞이지 않게 서버가 분리 표시
- 캐릭터: **내 캐릭터(`myCharacter`)는 보낸다(2026-09-25 결정 — 닉네임이 아닌 게임 정보라 식별성이 낮고 캐릭터별 정확도 비교에 유용).** 팀원 캐릭터·닉네임은 "팀원 정보 전송 안 함" 원칙에 따라 계속 제외
- `pvpSignals` 는 **이름 목록뿐**이다. 신호별 수치(궁 델타, 팀 전투 비율 등)가 없어 가중치 튜닝의 재료로는 약하다 — 현재 메타데이터에 있는 `pvpScore`·`enemyRingMean`·`ultimateDelta` 외에 신호별 값을 넣을지 정한다

**보내지 않는 것 유지**: `title`(캐릭터·지역 포함 자동 생성이지만 수동 편집 가능해 개인정보가 섞일 수 있음), `sessionDir`·`thumbnailPath` 등 경로, `matchStartUtc` 원문(시청 시각 → 필요하면 상대 시각만)

### 오류 로그·환경

서버 `LogEntry` 는 `ts`·`level`·`message` 뿐이고 `Environment` 는 버전·OS·해상도·코덱·판독 실패 통계뿐이다. 친구 배포에서 실제로 물었던 항목([friend-guide](friend-guide.md))과 대조하면 부족하다.

- 로그: 로거 이름(모듈), 예외 종류·스택(경로 제거 후)을 분리 필드로. 같은 오류를 묶어 셀 수 있게 **오류 지문**(모듈+예외 종류+발생 위치의 해시)
- 환경 추가: `mode`, CPU 모델·코어 수, GPU 이름, 메모리, 디스플레이 배율, 화면 비율, 하드웨어 디코딩 사용 여부(`hwaccel`), HEVC 재생 가능 여부(프록시 전환 여부), ffmpeg 빌드·사용한 프록시 인코더(`h264_mf` 등), 스팀 버퍼 분(`buffer minutes`), 로캘/타임존
- 성능: 경기 분석 소요 시간과 경기 길이 비율(게임 중 부하 영향 판단 — deploy §7-14), 프록시 생성 시간
- 판독 건강도: `readFailStats` 를 키 목록으로 확정(배지 판독불가 비율, K/A·지역명·일차 본보기 유무, 결과 화면 OCR 성공 여부, 프로필이 측정값인지 축척 추정인지). **게임 패치로 HUD 가 바뀌었을 때의 조기 경보**가 되게 한다

### 서버(`infra`) 쪽에 필요한 변경 메모 (D12 가 만든 앱 쪽 사실, 서버는 건드리지 않았다)

- 라벨 스키마에 `labelNote`(문자열, 앱이 이미 500자로 자름·앞뒤 공백 제거, 라벨이 없으면 없음)를 추가하고 허용 필드 목록에 넣는다. 서버도 500자 상한을 방어적으로 둔다.
- `userLabel` 허용값을 `combat`/`other` 로 바꾼다. 앱은 로컬에 `pvp`/`pve` 를 그대로 저장하므로 **전송 시 변환은 D10 클라이언트가 한다**(`pvp→combat`, `pve→other`).
- 자유 텍스트라 서버는 로그·집계에 `labelNote` 원문을 남기지 않는 편이 안전하다([privacy.md](privacy.md) 와 함께 확인).

### 한계 (의도적)

영상·화면 이미지를 보내지 않으므로 **"검출기가 왜 틀렸는가"를 프레임 단위로 재현하는 분석은 불가능**하다. 수치·라벨·판독 통계로 "어느 환경에서 얼마나 틀리는가"를 보는 것까지가 이 전송의 목적이다. 프레임 재현이 필요한 사례는 사용자가 직접 보내는 진단 zip(이미지 없음)이나 친구와 별도 협의로 다룬다.
