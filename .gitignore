# 미디어 / 스크린샷 — 저장소에 넣지 않는다
*.mp4
*.mkv
*.m4s
*.mpd
*.mov
*.avi
*.png
*.jpg
*.jpeg
*.gif
*.bmp
*.webp
!docs/**/*.png
!docs/**/*.jpg

# 단, 테스트용 소형 프레임 크롭은 예외로 저장소에 둔다
!tests/data/frames/*.png

# 본보기 라벨셋 원본(게임 화면에서 자른 이미지)과 라벨 — 공개 저장소에 넣지 않는다(개발 PC 에만 보관)
data/templates/**/_samples/
data/templates/digits/*_samples/
data/templates/**/*_labels.jsonl

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.venv/
venv/
build/
dist/

# 스크래치패드 / 임시
scratchpad/
*.tmp
*.raw

# 설정 / 로컬 상태 (실행 시 생성)
config.local.json
*.log

# 에디터
.vscode/
.idea/
*.swp

# 검토용 HTML (커밋하지 않는다)
review.html

# Playwright MCP 산출물
.playwright-mcp/

# 배포용 번들 바이너리 (tools/fetch_ffmpeg.py 가 받는다 — 커밋하지 않는다)
vendor/

# 빌드 때 환경변수에서 만드는 서버 토큰 파일 (공개 저장소에 올리지 않는다)
data/telemetry_endpoint.json

# tools/pull_labels.py 가 서버에서 받아 오는 라벨(개인 분석용)
pulled_labels/
