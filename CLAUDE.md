# 이 저장소에서 일할 때 지킬 것

## 문서를 최신 상태로 유지한다

- **실행 방법이 바뀌면 그때그때 [README.md](README.md) 의 "실행 방법" 섹션을 갱신한다.**
  새 CLI 진입점을 추가하거나, 기존 CLI 의 옵션/동작이 바뀌거나, 실행 순서(예:
  프론트 빌드 필요 여부)가 바뀌면 코드와 같은 커밋에서 README 도 같이 고친다.
  나중에 몰아서 하지 않는다 — 실행 방법 문서가 실제 코드와 어긋나 있으면
  나중에 이어서 작업할 때(세션이 끊긴 뒤 등) 잘못된 정보로 삽질하게 된다.
- 같은 원칙으로 [docs/SPEC.md](docs/SPEC.md) 는 설계 결정이 바뀔 때마다,
  [docs/plan.md](docs/plan.md) / [docs/plan-pipeline.md](docs/plan-pipeline.md) /
  [docs/plan-ui.md](docs/plan-ui.md) 는 작업 단계를 끝낼 때마다 진행 상태
  체크박스와 "확인 필요" 절을 갱신한다. 문서 갱신은 해당 작업 커밋에 포함시킨다.

## 개발 환경

- **파이썬은 항상 가상환경(`.venv`)에서 작업한다.** 시스템 파이썬에 직접
  설치하지 않는다 — 이 PC에는 다른 프로젝트(ultralytics 등)가 이미 깔려 있어
  버전 충돌이 난다.

## 구현 방식

- 중요한 기능은 테스트를 먼저 쓰는 TDD 로 진행한다.
- 불필요한 주석은 달지 않는다.
- git 에는 스크린샷/영상 등 미디어 파일을 넣지 않는다(`.gitignore` 참고).
  프로젝트 개발과 관련된 내용만 커밋한다.
- 커밋은 최대한 잦게 나눈다. 커밋 메시지는 한국어로, 무엇을 왜 바꿨는지와
  실측/테스트로 확인한 내용을 적는다.
